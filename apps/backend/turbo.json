{
  "extends": ["//"],
  "tasks": {
    "dev": {
      "with": ["@roar-platform/api-contract#dev"]
    },
    "build": {
      "env": [
        "DATABASE_URL",
        "CORE_DATABASE_URL",
        "ASSESSMENT_DATABASE_URL",
        "PORT",
        "KEEP_ALIVE_TIMEOUT",
        "FIREBASE_SERVICE_ACCOUNT_CREDENTIALS",
        "FIREBASE_AUTH_EMULATOR_HOST",
        "GOOGLE_APPLICATION_CREDENTIALS",
        "GCLOUD_PROJECT",
        "GOOGLE_CLOUD_PROJECT",
        "FGA_API_URL",
        "FGA_STORE_ID",
        "FGA_MODEL_ID",
        "FGA_OIDC_AUDIENCE"
      ]
    },
    "test": {
      "env": ["AUTHZ_MODEL_PATH"]
    },
    "lint": {
      "dependsOn": ["^@roar-platform/api-contract#build"]
    }
  }
}
