/**
 * Backend-owned filter types.
 *
 * Re-exported from the API contract for now. If the service layer needs a
 * different shape in the future, replace the re-export with a local definition.
 */
export type { ParsedFilter } from '@roar-dashboard/api-contract';
