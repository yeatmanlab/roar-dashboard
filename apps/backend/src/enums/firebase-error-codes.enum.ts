/**
 * Enum of Firebase error codes.
 */
export enum FirebaseErrorCode {
  USER_NOT_FOUND = 'auth/user-not-found',
  EMAIL_ALREADY_EXISTS = 'auth/email-already-exists',
  TOO_MANY_REQUESTS = 'auth/too-many-requests',
}
