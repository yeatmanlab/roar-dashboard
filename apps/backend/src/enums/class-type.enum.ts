/**
 * Class Type Enum
 *
 * Enumerates the available class types following the OneRoster spec.
 */
enum ClassType {
  HOMEROOM = 'homeroom',
  SCHEDULED = 'scheduled',
  OTHER = 'other',
}

export default ClassType;
