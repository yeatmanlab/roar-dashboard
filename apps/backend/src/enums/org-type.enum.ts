/**
 * Org Type Enum
 *
 * Enumerates the available org types.
 */
enum OrgType {
  DISTRICT = 'district',
  SCHOOL = 'school',
  LOCAL = 'local',
  STATE = 'state',
  REGION = 'region',
}

export default OrgType;
