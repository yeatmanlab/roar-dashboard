# Dev: leave empty to use the Vite proxy (/v1 → http://localhost:4000/v1).
# Production/staging: set to the API origin, with no path (e.g. https://api.roar.education).
# The version prefix comes from the contract.
ROAR_API_BASE_URL=

# ---------------------------------------------------- #
# Local Development Overrides
# ---------------------------------------------------- #
# BACKEND_URL=https://localhost:4000
