SENTRY_AUTH_TOKEN=

# Firebase config for local development.
# Staging and production deployments fetch this at runtime from Firebase Hosting.
# Contact a team member for the development project values.
FIREBASE_APP_API_KEY=
FIREBASE_APP_AUTH_DOMAIN=
FIREBASE_APP_PROJECT_ID=
FIREBASE_APP_STORAGE_BUCKET=
FIREBASE_APP_MESSAGING_SENDER_ID=
FIREBASE_APP_APP_ID=

# ROAR backend server root URL. The /v1 version prefix is appended automatically.
# Requires `dev:setup:certs` to be run first
ROAR_API_URL=https://localhost:4000
