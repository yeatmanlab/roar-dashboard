/**
 * Agreement type values, mirroring the backend `agreementType` enum
 * (`packages/api-contract` `AGREEMENT_TYPE_VALUES`).
 */
export const AGREEMENT_TYPES = Object.freeze({
  TOS: 'tos',
  ASSENT: 'assent',
  CONSENT: 'consent',
});
