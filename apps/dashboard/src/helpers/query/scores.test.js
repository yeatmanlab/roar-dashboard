import { describe, it } from 'vitest';

describe('query/scores', () => {
  describe('getAdministrationSupportCategories', () => {
    it.todo('fetches support categories from the API');
    it.todo('handles authentication errors');
    it.todo('handles network errors');
  });
});
