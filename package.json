{
  "name": "roar-dashboard",
  "private": true,
  "version": "3.13.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "prepare": "husky"
  },
  "devDependencies": {
    "prettier": "^3.6.0",
    "turbo": "^2.5.5"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@bdelab/roav-mep": "^1.2.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  }
}
